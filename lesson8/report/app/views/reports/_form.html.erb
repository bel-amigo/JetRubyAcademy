<%= form_with(model: report) do |form| %>
  <% if report.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(report.errors.count, "error") %> prohibited this report from being saved:</h2>

      <ul>
        <% report.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.label :user, style: "display: block" %>

    <%#= form.select :user, User.all.map { |user| [user.first_name + ' ' + user.last_name, user.id] }, { prompt: true }, { class: 'form-select' } %>
    <%= form.select :user_id, User.all.map { |user| ["#{user.first_name} #{user.last_name}", user.id] }, { prompt: true }, { class: 'form-select' } %>


  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
